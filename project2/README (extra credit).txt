Run the following command to start the vzip_visualizer.py program (extra credit): 
python3 vzip_visualizer.py

Make sure you have OpenCV installed for python.
If running Ubuntu, you can do install it with the following command:
sudo apt-get install python3-opencv